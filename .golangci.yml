linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - gofmt
run:
  timeout: 5m
  modules-download-mode: readonly
issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
